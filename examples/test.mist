func pow2(a, b) {
    if(b == 2) {
        return a * a;
    } else {
        return a / 3.141592653;
    };
};

have x = pow2(2, 2);

have i = 0;
have arr = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j'];
while(i < 10)  {
    print(arr[i]);
    i = i + 1;
};