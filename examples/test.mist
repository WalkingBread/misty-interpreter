func pow2(a, b) {
    func x() {
        return 2;
    };

    if(b == 2) {
        print(x());
        return a * a;
    } else {
        return a / 3.141592653;
    };
};

have x = pow2(2, 2);

have arr = [1, 2, 3];
arr[2] = 0;
print(arr);
