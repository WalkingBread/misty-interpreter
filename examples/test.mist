func foo(x, y) {
    if(x + y == 12) {
        print('yes');
    } else {
        print('no');
    };
};

have xx;

have z = [1, 2, 3];
have x = z[0];

have g = foo(1, 2);

func pow2(a) {
    return a * a;
};

have num = pow2(x);

